3.2.1

更新内容：

1. 日报归档统计修复
   - 修复归档文档计数逻辑：归档数不再受限于同期上传数
   - 归档率分母改为全量已上传文档数，正确反映归档进度

2. 通知类型选择
   - 新建/编辑定时任务增加"邮件"和"站内信"通知方式复选框
   - 可单独控制是否发送邮件或站内信

3. 文档列表状态即时刷新
   - 归档、撤销归档、审批等操作后自动重新加载项目数据
   - 文档状态（已归档/待审批等）即时更新，无需手动刷新

4. 执行记录详情查看
   - 定时任务发送记录支持点击查看详情
   - 详情包含邮件收件人地址、站内信收件人列表、发送状态

5. 迁移脚本
   - 0012: scheduled_tasks 表新增 email_enabled 字段

版本号: 2026042002

---
以下为历史版本：

3.2.0

更新内容：

1. Dashboard 看板布局优化
   - 验收情况和资料完整两个统计图表并排横向显示，减少纵向滚动
   - 响应式布局：窄屏自动切换为单列

2. 文档变化统计接入真实数据
   - 替换原有随机假数据为数据库真实记录
   - 支持按日/3天/7天/30天查看文档新增、更新、归档变化
   - 点击列表项可跳转到对应项目文档

3. 定时任务增强
   - 跳过周末/节假日选项对所有周期频率生效（原仅日报）
   - 任务列表新增"下一次执行时间"列
   - 任务操作栏新增"跳过下次"按钮，可临时跳过下一次执行
   - 节假日数据支持在线获取（timor.tech API）

4. 乙方单位选择改进
   - 新建/编辑项目时乙方单位改为下拉选择（从承建单位列表）
   - 支持新建承建单位，新建后需PMO审批
   - 承建单位管理新增"状态"列和审批操作按钮

5. 刷新问题修复
   - 项目编辑保存后侧栏列表即时刷新
   - 项目/文档列表增加 Cache-Control 防止浏览器缓存

6. 迁移脚本
   - 0011: organizations 表新增 status 字段（承建单位审批状态）

版本号: 2026042001

---
以下为历史版本：

3.1.0

更新内容：

1. 定时任务调度器 _tick() 关键 bug 修复
   - 修复 _tick() 中 continue 后代码永远不可达的缩进错误，定时任务现在可以正常自动执行

2. 自动化增量迁移框架
   - 新增 tools/migrate/ 增量迁移框架（runner.py），支持自动发现并执行编号迁移脚本
   - Daemon.sh upgrade 自动调用增量迁移，无需每次手动修改 Daemon.sh
   - 新增迁移脚本编写指南 tools/migrate/README.md

3. 定时日报跳过非工作日功能
   - 新增中国法定节假日日历服务（china_holidays.py），覆盖 2025-2027 年
   - 日报类型任务支持"跳过非工作日"选项，法定节假日及周末不执行
   - 迁移脚本 0010: scheduled_tasks 表新增 skip_holidays 字段

4. 定时任务管理弹窗 UI 优化
   - Modal 窗口仅支持纵向拖拽调整大小（去除横向 resize）
   - 新增项目复选框筛选，支持 localStorage 记忆上次选中的项目
   - 新增按周期（日报/周报/月报）筛选下拉框
   - 筛选区域从 2 列改为 3 列布局

5. 编辑器弹窗增强
   - 频率选择"每日"时显示"跳过非工作日"复选框

版本号: 2026041801

---
以下为历史版本：

3.0.3

更新内容：

1. 周期状态刷新修复
   - 修复周期状态计算优先级，文档全部归档后可正确显示“已归档”
   - 修复周期导航项活跃样式冲突，避免刷新后字体大小不一致

2. 版本与文档同步
   - README 更新至 v3.0.3，并补充本次更新日志
   - 项目说明文档同步刷新版本标识与更新时间

3. 升级链路增强
   - `Daemon.sh upgrade` 继续保留自动迁移能力，并完善为“数据库 + 运行时数据文件”联动迁移
   - 升级后自动规范化 `uploads/tasks/report_schedules.json` 任务结构，兼容旧版单任务/字段缺失数据

4. 数据库迁移版本升级
   - 迁移版本提升至 v9
   - 新增常用查询索引：
     - `idx_messages_receiver_read_created`
     - `idx_archive_approvals_project_status_created`
   - `tools/migrate_db.py` 与 `tools/migrate_branch.py` 迁移版本保持一致

5. 隐私与示例清理
   - 诊断脚本移除硬编码真实项目名，改为命令行参数输入
   - 默认示例值统一为“示例项目”

6. 文档体系补齐
   - 新增工程说明：`docs/PROJECT_ENGINEERING_GUIDE.md`
   - 新增产品手册：`docs/PRODUCT_MANUAL.md`
   - README 增加升级迁移说明与文档索引

版本号: 2026041701
