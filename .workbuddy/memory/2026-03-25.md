# 2026-03-25 ZIP上传记录功能修复

## 问题描述

1. **ZIP上传记录不显示**：上传ZIP文件后，在"重新匹配文件管理"模态框中无法显示上传记录
2. **编辑按钮错误**：点击文档列表后的"编辑"按钮，弹出了错误的模态框且无法关闭
3. **上传记录未保存**：ZIP文件上传后，虽然文件被正确解压到项目目录，但上传记录未保存到项目的JSON文件中

## 修复内容

### 1. 修复ZIP上传记录保存功能

**文件**: `app/routes/document_routes.py`

**修改点**:
- 在 `upload_zip` 函数中，ZIP解压成功后，自动保存上传记录到项目的JSON文件中
- 生成唯一的ZIP记录ID（格式：`zip_YYYYMMDDHHMMSS`）
- 记录包含：文件名、相对路径、文件数量、上传时间、状态
- 使用 `json_file_manager.add_zip_upload_record()` 保存到项目JSON文件
- 路径转换为相对于项目uploads目录的相对路径

**代码逻辑**:
```python
# 如果解压成功，保存ZIP上传记录到项目JSON
if result.get('status') == 'success' and project_config:
    project_id = project_config.get('id')
    if project_id:
        # 生成ZIP记录ID
        zip_id = f"zip_{datetime.now().strftime('%Y%m%d%H%M%S')}"
        
        # 计算相对路径
        # ... 路径处理逻辑 ...
        
        # 构建ZIP上传记录
        zip_record = {
            'id': zip_id,
            'name': original_filename,
            'path': path_for_record,
            'file_count': result.get('total_files', 0),
            'upload_time': datetime.now().isoformat(),
            'status': '已完成'
        }
        
        # 保存到项目JSON文件
        project_file = os.path.join(doc_manager.config.projects_base_folder, f"{project_id}.json")
        success = json_file_manager.add_zip_upload_record(project_file, zip_record)
```

### 2. 修复编辑按钮功能

**文件**: `static/js/modules/document.js`

**修改点**:
- 将文档列表中"编辑"按钮的点击事件从 `openEditModal` 改为 `openMaintainModal`
- 添加 `openMaintainModal` 函数，打开文档上传/选择模态框并自动切换到"维护已选择文档"标签页
- 将 `openMaintainModal` 函数暴露到全局范围

**代码逻辑**:
```javascript
// 打开维护模态框（切换到维护标签页）
export function openMaintainModal(cycle, docName) {
    appState.currentCycle = cycle;
    appState.currentDocument = docName;
    // 打开第二个标签页（维护已选择文档）
    document.querySelectorAll('.main-tab-btn').forEach(btn => btn.classList.remove('active'));
    document.querySelectorAll('.main-tab-content').forEach(content => content.style.display = 'none');
    document.querySelector('.main-tab-btn[data-tab="maintain"]').classList.add('active');
    document.getElementById('maintainTab').style.display = 'block';
    
    // 加载维护页面的文档
    loadMaintainDocuments();
    
    // 打开模态框
    openModal(elements.documentModal);
}
```

### 3. 修复API参数传递

**文件**: `static/js/modules/api.js`

**修改点**:
- 修复 `loadZipRecords` 函数，添加 `projectId` 参数
- 修复 `deleteZipRecord` 函数，添加 `projectId` 参数

**文件**: `static/js/modules/project.js`

**修改点**:
- 修复 `loadZipRecords` 函数，调用API时传递 `project_id` 参数
- 修复 `handleDeleteZipRecord` 函数，调用API时传递 `project_id` 参数

**文件**: `static/js/main.js`

**修改点**:
- 修复 `handleDeleteZipRecord` 函数的参数顺序

## 实现功能

1. **ZIP上传记录自动保存**：上传ZIP文件后，自动将上传记录保存到项目的JSON文件中
2. **ZIP上传记录显示**：在"重新匹配文件管理"模态框中正确显示所有ZIP上传记录
3. **编辑按钮正常工作**：点击文档列表后的"编辑"按钮，正确打开维护已选择文档的模态框
4. **路径规范化**：ZIP上传记录中的路径使用相对于项目uploads目录的相对路径，便于后续处理

## 测试验证

1. 上传新的ZIP文件
2. 检查项目JSON文件中是否正确保存了 `zip_uploads` 字段
3. 打开"重新匹配文件管理"模态框，确认上传记录显示正确
4. 点击文档列表后的"编辑"按钮，确认打开正确的维护页面

## 文件变更

- `app/routes/document_routes.py` - 添加ZIP上传记录保存逻辑
- `static/js/modules/document.js` - 添加openMaintainModal函数，修复编辑按钮
- `static/js/modules/api.js` - 修复API参数传递
- `static/js/modules/project.js` - 修复API调用参数
- `static/js/main.js` - 修复全局函数参数
